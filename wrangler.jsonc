{
  // Worker 的名字，必须和 Cloudflare CI 里一致
  "name": "minbbs",

  // Cloudflare 要求必须指定 compatibility_date
  // 建议填今天或近期日期，保证兼容性
  "compatibility_date": "2026-06-24",

  // 如果你只是前端项目，需要托管静态资源，
  // 那就指定构建产物目录，比如 build/client
  "assets": {
    "directory": "./build/client",
  },
}
